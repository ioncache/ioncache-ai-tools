{
  "event": "PreToolUse",
  "toolNames": ["Bash"],
  "matcher": {
    "type": "regex",
    "field": "tool_input.command",
    "pattern": "(^|[\\s;&|(`<>])(?:[\\w./-]*/)?(kill|pkill|killall)($|[\\s;&|)`<>])"
  },
  "action": "deny",
  "message": "Never run kill/pkill/killall without asking the user first, even for your own leftover process. Ask, then wait for an explicit yes. If this is genuinely getting in your way, disable this rule via .claude/ioncache-ai-tools.local.json or Codex config (see README's Disabling a rule section)."
}
